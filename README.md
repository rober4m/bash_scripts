# Scripts and algorithms to work in Linux

Before running the script:
`chmod +x sysinfo.sh`

## Basic info

This script reads the basic info of a Linux computer such as, cores, ram, disk space and availability. 
- sysinfo.sh
The script also writes that info in a md file in the same folder where the script is run. 